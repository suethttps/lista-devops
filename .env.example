# Variáveis OPCIONAIS para desenvolvimento local.
# Por padrão o app conecta no Postgres do docker-compose.yml (localhost:5432)
# com usuário/senha "postgres". Só copie para .env se precisar sobrescrever.

# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
